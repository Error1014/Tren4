<Page x:Class="Tren4.Pages.AutorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Tren4.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AutorizationPage">

    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <DockPanel >
                <TextBlock  Text="Логин: " Style="{StaticResource AutorizationFont}" Width="100"/>
                <TextBox x:Name="LoginBox"  Style="{StaticResource AutorizationFont}"/>
            </DockPanel>
            <DockPanel >
                <TextBlock Text="Пароль: "  Style="{StaticResource AutorizationFont}" Width="100"/>
                <PasswordBox x:Name="PasswordBox"  Style="{StaticResource AutorizationFont}"/>
            </DockPanel>
            <Button Content="Войти" Margin="20" Style="{StaticResource ButtonAccent}" Click="Whod"/>
            <TextBlock Style="{StaticResource NoAccentText}" Text="Для входа в систему введите логин 1 и пароль 1"/>
        </StackPanel>
        

    </Grid>
</Page>
